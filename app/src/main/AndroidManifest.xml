<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MeSetting.MyCourse"></activity>
        <activity android:name=".MeSetting.MyMessage" />
        <activity android:name=".MeSetting.MyRecord" />
        <activity android:name=".Fragment.SettingFragment" />
        <activity android:name=".Fragment.ScanFragment" />
        <activity android:name=".Fragment.MainPageFragment" />
        <activity android:name=".TabActivity" />
        <activity android:name=".FrogetActivity" />
        <activity android:name=".RegeditActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>